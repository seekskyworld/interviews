	git clone https://github.com/seekskyworld/interviews.git//一，首次使用,把github上面的仓库克隆到本地
	git init					      //一，首次使用,创建仓库
git add .						     //二，如果是克隆的仓库，需要进入仓库文件夹再add					
git commit -m "首次提交，添加相关文件"		
	ssh-keygen -t rsa -C "xxxxxx@gmail.com" //首次使用，把C:\Users\用户\id_rsa.pub这个填进github，sshkey页面，只用一次就行
	git remote add origin git@github.com:seekskyworld/interviews.git  //首次使用，通过ssh连接，直接检验本地ssh变量
git push origin main //如果远程仓库为空 git push -u origin main(master)

